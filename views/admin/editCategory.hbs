<div class="content">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <!-- general form elements -->
        <div class="card card-primary">
          <form
            action="/admin/categories/edit/{{category.id}}"
            method="POST"
            enctype="multipart/form-data"
          >
            <div class="card-body">
              <div class="form-group">
                <label for="name">Kategoriya nomi</label>
                <input
                  type="text"
                  class="form-control"
                  id="name"
                  placeholder="Nomi"
                  name="name"
                  value="{{category.name}}"
                />
              </div>
              <div class="form-group">
                <label for="icon">Kategoriya ikonkasi</label>
                <div class="input-group">
                  <div class="custom-file">
                    <input
                      type="file"
                      class="custom-file-input"
                      id="icon"
                      name="icon"
                    />
                    <label class="custom-file-label" for="icon">Kategoriya
                      ikonkasi</label>
                  </div>
                </div>
              </div>
            </div>
            <!-- /.card-body -->
            <div class="card-footer">
              <button type="submit" class="btn btn-primary">Yangilash</button>
            </div>
          </form>
        </div>
        <!-- /.card -->
      </div>
    </div>
  </div>
</div>